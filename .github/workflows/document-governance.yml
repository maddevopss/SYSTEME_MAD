name: Document governance

on:
  pull_request:
    branches: [main]
    paths: ["**/*.md", "08-BOOTSTRAPS/audit-document-governance.py", ".github/workflows/document-governance.yml"]
  push:
    branches: [main]
    paths: ["**/*.md", "08-BOOTSTRAPS/audit-document-governance.py", ".github/workflows/document-governance.yml"]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: document-governance-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Auditer la cohérence documentaire
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.before }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
        run: python 08-BOOTSTRAPS/audit-document-governance.py
